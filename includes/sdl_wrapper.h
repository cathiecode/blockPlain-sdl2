int size(int w, int h);

/* Processing compatible functions */
extern int width, height;
extern int mouseX, mouseY;

void background(int r, int g, int b);
void rect(int x1, int y1, int x2, int y2);
void draw_done();
